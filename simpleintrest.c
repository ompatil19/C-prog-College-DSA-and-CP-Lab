#include <stdio.h>
void main(){ // si p t r 
   float si,r;
   int p,t;
   printf("enter the value of p t r respectively \n");
   scanf("%d", &p);
   scanf("%d", &t);
   scanf("%f", &r);
   si=(p*t*r)/100;
   printf("%f", si);
}